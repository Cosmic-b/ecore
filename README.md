# ecore

ScanFor(path, format) // format can be ".***" or "dir"
    scans dir for ...

ListDiff(a, b)
    b list minus a

OneGlob(input)
    returns one path of file if there is only one after globbinig, err
